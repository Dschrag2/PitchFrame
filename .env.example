# Copy this file to .env and fill in real values. Never commit .env itself.

# Database (Postgres via docker-compose.yml)
POSTGRES_USER=pitchframe
POSTGRES_PASSWORD=changeme
POSTGRES_DB=pitchframe
DATABASE_URL=postgresql://pitchframe:changeme@localhost:5433/pitchframe

# Web backend
API_SECRET_KEY=changeme

# Optional: if you add any paid/keyed data source later
# THIRD_PARTY_API_KEY=
